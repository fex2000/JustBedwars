<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JustBedwars.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustBedwars.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" VerticalAlignment="Center" Padding="50" MaxWidth="1000">
        <TextBlock Text="Settings" FontWeight="SemiBold" FontSize="30" HorizontalAlignment="Center" Margin="20"/>
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="{ThemeResource ControlCornerRadius}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid Padding="12" ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Grid.Column="0" Grid.Row="0">
                    <TextBlock Text="Hypixel API Key"/>
                    <TextBlock Text="Use at your own risk!" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
                <PasswordBox x:Name="ApiKeyPasswordBox" Height="32" Grid.Column="1" Grid.Row="0" PasswordChanged="ApiKeyPasswordBox_PasswordChanged"/>
            </Grid>
        </Border>
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="{ThemeResource ControlCornerRadius}" Margin="0,10,0,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid Padding="12" ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Grid.Column="0" Grid.Row="0">
                    <TextBlock Text="Log Location"/>
                    <TextBlock Text="Change if you aren't playing in the Vanilla Launcher" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                </StackPanel>
                <TextBox x:Name="LogFilePathTextBox" IsReadOnly="True" VerticalAlignment="Center" Grid.Column="1"/>
                <Button x:Name="SelectLogFileButton" Content="Select" Grid.Column="2" Click="SelectLogFileButton_Click"/>
            </Grid>
        </Border>

        <Expander Margin="0,10,0,0">
            <Expander.Header>
                <StackPanel VerticalAlignment="Center" Grid.Column="0" Grid.Row="0">
                    <TextBlock Text="Advanced Settings"/>
                    <TextBlock Text="For Development" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                </StackPanel>
            </Expander.Header>
            <Expander.Content>
                <Button Content="Open Debug Window" Click="OpenDebugWindow_Click" Margin="0,10,0,0"/>
            </Expander.Content>
        </Expander>
    </StackPanel>
</Page>
