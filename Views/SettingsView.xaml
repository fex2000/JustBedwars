<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JustBedwars.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustBedwars.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" VerticalAlignment="Center" Padding="50" MaxWidth="1000">
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="{ThemeResource ControlCornerRadius}">
            <Grid Padding="12" ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="250"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Grid.Column="0" Grid.Row="0">
                    <TextBlock Text="Hypixel API Key"/>
                    <TextBlock Text="Use at your own risk!" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
                <PasswordBox Height="32" Grid.Column="1" Grid.Row="0"/>
            </Grid>
        </Border>
    </StackPanel>
</Page>
