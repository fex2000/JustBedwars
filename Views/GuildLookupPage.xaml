<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JustBedwars.Views.GuildLookupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustBedwars.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8">
            <TextBlock Text="Search by"
                       FontWeight="SemiBold"
                       FontSize="16"/>
            <controls:Segmented x:Name="SearchTypeSegmented" SelectionChanged="SearchType_SelectionChanged" SelectedIndex="0">
                <controls:SegmentedItem Content="Guild Name"/>
                <controls:SegmentedItem Content="Guild ID"/>
                <controls:SegmentedItem Content="Member Name"/>
                <controls:SegmentedItem Content="Member UUID"/>
            </controls:Segmented>

            <AutoSuggestBox x:Name="SearchAutoSuggestBox" 
                          PlaceholderText="Search..." 
                          QueryIcon="Find"
                          TextChanged="SearchAutoSuggestBox_TextChanged"
                          QuerySubmitted="SearchAutoSuggestBox_QuerySubmitted"/>
        </StackPanel>
    </Grid>
</Page>
