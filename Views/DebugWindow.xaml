<Window
    x:Class="JustBedwars.Views.DebugWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustBedwars.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Height="85" VerticalAlignment="Top" Grid.Row="0">
            <TextBlock Text="Debug Logs" FontWeight="SemiBold" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10"/>
        </Border>
        <ScrollView Grid.Row="1">
            <ScrollView.Content>
                <TextBlock x:Name="DebugTextBox" TextWrapping="Wrap" Margin="0, 10, 0, 0" IsTextSelectionEnabled="True"/>
            </ScrollView.Content>
        </ScrollView>
        <StackPanel Orientation="Horizontal" Margin="5" VerticalAlignment="Bottom" HorizontalAlignment="Right" Spacing="8">
            <TextBox PlaceholderText="Emulate Players" x:Name="EmulateUsername"/>
            <Button Content="Joined" Click="ButtonJoined_Click"/>
            <Button Content="Left" Click="ButtonLeft_Click"/>
        </StackPanel>
    </Grid>
</Window>
