<Page
    x:Class="JustBedwars.Views.LeaderboardsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustBedwars.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <SelectorBar x:Name="LeaderboardSelector" SelectionChanged="LeaderboardSelector_SelectionChanged">
                    <SelectorBarItem x:Name="StarsItem" Text="Stars" IsSelected="True"/>
                    <SelectorBarItem x:Name="WinsItem" Text="Wins"/>
                    <SelectorBarItem x:Name="FinalsItem" Text="Finals"/>
                </SelectorBar>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="8" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    Click players for their Stats!
                </TextBlock>
            </Grid>
            <SelectorBar x:Name="TimeFilterSelector" SelectionChanged="TimeFilterSelector_SelectionChanged" Visibility="Collapsed">
                <SelectorBarItem x:Name="WeeklyItem" Text="Weekly" IsSelected="True"/>
                <SelectorBarItem x:Name="LifetimeItem" Text="Lifetime"/>
                <animations:Implicit.ShowAnimations>
                    <animations:OpacityAnimation Duration="0:0:0.5" From="0" To="1"/>
                </animations:Implicit.ShowAnimations>
                <animations:Implicit.HideAnimations>
                    <animations:OpacityAnimation Duration="0:0:0.5" From="1" To="0"/>
                </animations:Implicit.HideAnimations>
            </SelectorBar>
        </StackPanel>

        <ListView x:Name="LeaderboardList" Grid.Row="1" IsItemClickEnabled="True" ItemClick="LeaderboardList_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Rank}" Margin="0,0,12,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}" />
                        <TextBlock Grid.Column="2" Text="{Binding Value}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ProgressRing x:Name="LoadingIndicator" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="50" Height="50" IsActive="False"/>
    </Grid>
</Page>