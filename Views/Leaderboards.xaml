<Page
    x:Class="JustBedwars.Views.LeaderboardsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustBedwars.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    xmlns:media="using:CommunityToolkit.WinUI.Media"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="6">
            <Grid ColumnSpacing="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <controls:Segmented SelectedIndex="0"
                                    Canvas.ZIndex="1"
                                    SelectionChanged="LeaderboardSelector_SelectionChanged"
                                    x:Name="LeaderboardSelector"
                                    Grid.Column="0">
                    <controls:SegmentedItem Content="Level" Width="100"/>
                    <controls:SegmentedItem Content="Wins"/>
                    <controls:SegmentedItem Content="Finals"/>
                </controls:Segmented>

                <Grid Grid.Column="1">
                    <Grid.Clip>
                        <RectangleGeometry Rect="0,0,205,50"/>
                    </Grid.Clip>
                    <controls:Segmented x:Name="TimeFilterSelector"
                                        SelectionChanged="TimeFilterSelector_SelectionChanged"
                                        Visibility="Collapsed"
                                        Margin="4,0,0,0"
                                        SelectedIndex="0">
                        <controls:SegmentedItem Content="Weekly" Width="100"/>
                        <controls:SegmentedItem Content="Lifetime"/>

                        <animations:Implicit.ShowAnimations>
                            <animations:TranslationAnimation Duration="0:0:0.5" From="-206,1,0" To="0"/>
                        </animations:Implicit.ShowAnimations>
                        <animations:Implicit.HideAnimations>
                            <animations:TranslationAnimation Duration="0:0:0.5" From="0" To="-206,1,-1"/>
                        </animations:Implicit.HideAnimations>
                    </controls:Segmented>
                </Grid>

                <TextBlock Grid.Column="2"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Margin="8"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    Click players for their Stats!
                </TextBlock>
            </Grid>
        </StackPanel>

        <ListView x:Name="LeaderboardList" Grid.Row="1" IsItemClickEnabled="True" ItemClick="LeaderboardList_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{Binding Rank}"
                                   Margin="0,0,12,0"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                        <labs:Shimmer Grid.Column="1"
                                      Height="40"
                                      Width="40"
                                      HorizontalAlignment="Left"/>
                        <Image Grid.Column="1"
                               Source="{Binding PlayerImageIconUri}"
                               Height="40"
                               Width="40"
                               Margin="0,0,16, 0">
                        </Image>
                        <TextBlock Grid.Column="2"
                                   Text="{Binding Name}"
                                   VerticalAlignment="Center"
                                   FontSize="16"/>
                        <TextBlock Grid.Column="3"
                                   VerticalAlignment="Center"
                                   Text="{Binding Value}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ProgressRing x:Name="LoadingIndicator" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="50" Height="50" IsActive="False"/>
    </Grid>
</Page>