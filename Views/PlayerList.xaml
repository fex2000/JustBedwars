<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JustBedwars.Views.PlayerList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustBedwars.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="10" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Title -->
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="{ThemeResource ControlCornerRadius}" Grid.Row="0">
            <Grid VerticalAlignment="Center" Padding="15, 0" ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Player" Grid.Column="0"/>
                <TextBlock Text="FKDR" Grid.Column="1"/>
                <TextBlock Text="WLR" Grid.Column="2"/>
                <TextBlock Text="Finals" Grid.Column="3"/>
                <TextBlock Text="Wins" Grid.Column="4"/>
            </Grid>
        </Border>
        <!-- Players -->
        <ListView x:Name="PlayersListView" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Player">
                    <Grid Padding="15, 10" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                                                <TextBlock Text="{x:Bind Username}" Grid.Column="0"/>
                        <TextBlock Text="{x:Bind FKDR}" Grid.Column="1"/>
                        <TextBlock Text="{x:Bind WLR}" Grid.Column="2"/>
                        <TextBlock Text="{x:Bind Finals}" Grid.Column="3"/>
                        <TextBlock Text="{x:Bind Wins}" Grid.Column="4"/>
                        <ProgressRing IsActive="{x:Bind IsLoading}" Grid.ColumnSpan="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
