<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JustBedwars.Views.PlayerList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustBedwars.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="10" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Title -->
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="{ThemeResource ControlCornerRadius}" Grid.Row="0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid VerticalAlignment="Center" Padding="15, 0" ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Player" Grid.Column="0"/>
                <TextBlock Text="Tag" Grid.Column="1"/>
                <TextBlock Text="FKDR" Grid.Column="2" TextAlignment="Center"/>
                <TextBlock Text="WLR" Grid.Column="3" TextAlignment="Center"/>
                <TextBlock Text="Finals" Grid.Column="4" TextAlignment="Center"/>
                <TextBlock Text="Wins" Grid.Column="5" TextAlignment="Center"/>
                <TextBlock Text="Stars" Grid.Column="6" TextAlignment="Center"/>
            </Grid>
        </Border>
        <!-- Players -->
        <ListView x:Name="PlayersListView" Grid.Row="1" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Player">
                    <Grid Padding="0, 10" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Username}" Grid.Column="0" Visibility="{x:Bind IsContentVisible}"/>
                        <TextBlock Text="{x:Bind PlayerTag}" Grid.Column="1" Visibility="{x:Bind IsContentVisible}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind FKDR}" Grid.Column="2" Visibility="{x:Bind IsContentVisible}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind WLR}" Grid.Column="3" Visibility="{x:Bind IsContentVisible}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind Finals}" Grid.Column="4" Visibility="{x:Bind IsContentVisible}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind Wins}" Grid.Column="5" Visibility="{x:Bind IsContentVisible}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind Star}" Grid.Column="6" Visibility="{x:Bind IsContentVisible}" TextAlignment="Center"/>
                        <ProgressBar Grid.ColumnSpan="5" Grid.Column="3" IsIndeterminate="True" Visibility="{x:Bind IsLoaderVisible}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
